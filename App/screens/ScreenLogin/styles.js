// @flow

import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    registerButtonWrap: {
        // width: '100%',
        alignItems: 'center',
        marginTop: 64,
        // position: 'absolute',
        // bottom: 56
    }
})

export default styles
