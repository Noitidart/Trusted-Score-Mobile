// @flow

import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    bottomButtonWrap: {
        // alignItems: 'center',
        alignSelf: 'center',
        width: '50%'
    }
})

export default styles
